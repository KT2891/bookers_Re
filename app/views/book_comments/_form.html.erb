<%= form_with model: [book, book_comment], local: false do |f|%>
  <div class="form-group">
    <%= f.text_area :comment, class: "form-control", :size=>"40x10" %>
  </div>
  <div class="form-group">
    <%= f.submit "送信", class: "btn btn-outline-dark" %>
  </div>
<% end %>